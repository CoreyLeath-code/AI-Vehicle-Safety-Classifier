apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: vehicle-safety-default-deny
spec:
  podSelector:
    matchLabels:
      app: vehicle-safety-classifier
  policyTypes: [Ingress, Egress]
  ingress:
    - ports:
        - {protocol: TCP, port: 5000}
